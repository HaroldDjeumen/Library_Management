<Page
    x:Class="Librarymanage.Admin_Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Librarymanage"
    xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Admin_Page"
    d:DesignHeight="661"
    d:DesignWidth="984"
    mc:Ignorable="d">

    <Grid Background="#9370DB">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Sidebar Navigation  -->
        <Border
            Grid.Column="0"
            Margin="0,10,5,10"
            Background="#4B0082"
            CornerRadius="0,10,10,0">
            <StackPanel
                Margin="0,30,0,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Top">
                <TextBlock
                    Margin="0,0,0,40"
                    HorizontalAlignment="Center"
                    FontSize="30"
                    FontWeight="Bold"
                    Foreground="#ECF0F1"
                    Text="ADMIN PANEL" />

                <RadioButton
                    Margin="0,10,0,0"
                    Click="BooksButton_Click"
                    Content="Books "
                    FontSize="20"
                    Foreground="White"
                    Style="{StaticResource MenuButtonTheme}" />
                <RadioButton
                    Margin="0,10,0,0"
                    Click="MembersButton_Click"
                    Content="Members "
                    FontSize="20"
                    Foreground="White"
                    Style="{StaticResource MenuButtonTheme}" />
          
                <RadioButton
                    Margin="0,10,0,0"
                    Click="EventsButton_Click"
                    Content="Events "
                    FontSize="20"
                    Foreground="White"
                    Style="{StaticResource MenuButtonTheme}" />
                <RadioButton
                    Margin="0,10,0,0"
                    Click="StatsButton_Click"
                    Content="Statistics"
                    FontSize="20"
                    Foreground="White"
                    Style="{StaticResource MenuButtonTheme}" />
                <RadioButton
                    x:Name="Go_Back"
                    Margin="0,290,0,0"
                    Click="BackButton_Click"
                    Content="Go Back"
                    FontSize="20"
                    Foreground="White"
                    Style="{StaticResource MenuButtonTheme}" />
            </StackPanel>
        </Border>

        <!--  Main Content Area  -->
        <Border
            Grid.Column="1"
            Margin="5,10,10,10"
            Background="#E6E6FA"
            BorderBrush="#BDC3C7"
            BorderThickness="1"
            CornerRadius="10"
            SnapsToDevicePixels="True">
            <Grid
                x:Name="ContentPanel"
                Margin="0,10,0,10"
                Background="Transparent">
                <Grid.Resources>
                    <Style x:Key="AdminNavButton" TargetType="Button">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground" Value="#ECF0F1" />
                        <Setter Property="FontSize" Value="18" />
                        <Setter Property="Height" Value="50" />
                        <Setter Property="Margin" Value="0,5" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Left" />
                        <Setter Property="Padding" Value="20,0,0,0" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#34495E" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    <Style x:Key="SectionHeader" TargetType="TextBlock">
                        <Setter Property="FontSize" Value="24" />
                        <Setter Property="FontWeight" Value="Bold" />
                        <Setter Property="Foreground" Value="#9370DB" />
                        <Setter Property="Margin" Value="0,0,0,20" />
                    </Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Height" Value="35" />
                        <Setter Property="Padding" Value="5" />
                        <Setter Property="BorderBrush" Value="#BDC3C7" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                    </Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#3498DB" />
                        <Setter Property="Foreground" Value="White" />
                        <Setter Property="Padding" Value="10,5" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Cursor" Value="Hand" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980B9" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Resources>

                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Style="{StaticResource SectionHeader}"
                    Text="Welcome to the Admin Dashboard" />

                <!--  Books Management Panel  -->
                <ScrollViewer
                    x:Name="BookAdminPanel"
                    VerticalScrollBarVisibility="Auto"
                    Visibility="Collapsed">
                    <ScrollViewer.Resources>
                        <Style BasedOn="{StaticResource ModernScrollBarStyle}" TargetType="ScrollBar" />
                    </ScrollViewer.Resources>
                    <StackPanel Margin="10">
                        <TextBlock Style="{StaticResource SectionHeader}" Text="Add New Book" />
                        <StackPanel Margin="0,5" Orientation="Horizontal">
                            <TextBox
                                x:Name="AdminSearchBox"
                                Width="300"
                                Height="30"
                                Style="{StaticResource ModernTextbox}"
                                Tag="Add books by name or author..." />
                            <Button
                                Width="100"
                                Background="#9370DB"
                                Click="AdminSearchButton_Click"
                                Content="Search " />
                        </StackPanel>

                        <Border
                            MinHeight="30"
                            Margin="0,20,0,0"
                            BorderBrush="#BDC3C7"
                            BorderThickness="1"
                            CornerRadius="5">
                            <ScrollViewer MaxHeight="470" VerticalScrollBarVisibility="Auto">
                                <StackPanel x:Name="ApiBooksPanel" />
                            </ScrollViewer>
                        </Border>

                        <TextBlock
                            Margin="0,40,0,0"
                            Style="{StaticResource SectionHeader}"
                            Text="Books in Database" />
                        <Border
                            BorderBrush="#BDC3C7"
                            BorderThickness="1"
                            CornerRadius="5">
                            <ScrollViewer
                                Width="668"
                                MaxHeight="560"
                                Margin="10,5,5,5"
                                VerticalScrollBarVisibility="Auto">
                                <StackPanel x:Name="DatabaseBooksPanel" />
                            </ScrollViewer>
                        </Border>
                    </StackPanel>
                </ScrollViewer>


                <!--  Statistics Panel  -->
                <ScrollViewer
                    x:Name="StatAdminPanel"
                    Width="710"
                    Margin="7,0,0,0"
                    Background="#E6E6FA"
                    Visibility="Collapsed">
                    <ScrollViewer.Resources>
                        <Style BasedOn="{StaticResource ModernScrollBarStyle}" TargetType="ScrollBar" />
                    </ScrollViewer.Resources>
                    <StackPanel>
                        <TextBlock
                            Margin="0,0,0,15"
                            Style="{StaticResource SectionHeader}"
                            Text="Library Statistics" />

                        <!--  New Members Row  -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="290" />
                                <!--  Left: Filters & Details  -->
                                <ColumnDefinition Width="*" />
                                <!--  Right: Chart  -->
                            </Grid.ColumnDefinitions>

                            <!--  Left Side: Filters + Details  -->
                            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                <TextBlock
                                    Margin="0,0,0,10"
                                    FontWeight="SemiBold"
                                    Text="Filter New Members" />

                                <StackPanel Margin="0,0,0,5" Orientation="Horizontal">
                                    <ComboBox
                                        Width="80"
                                        ItemsSource="{Binding Days}"
                                        SelectedItem="{Binding SelectedMemberDay}"
                                        Style="{StaticResource CustomComboBoxStyle}" />
                                    <ComboBox
                                        Width="80"
                                        Margin="15,0"
                                        ItemsSource="{Binding Months}"
                                        SelectedItem="{Binding SelectedMemberMonth}"
                                        Style="{StaticResource CustomComboBoxStyle}" />
                                    <ComboBox
                                        Width="80"
                                        ItemsSource="{Binding Years}"
                                        SelectedItem="{Binding SelectedMemberYear}"
                                        Style="{StaticResource CustomComboBoxStyle}" />
                                </StackPanel>

                                <TextBlock
                                    Margin="0,10,0,5"
                                    FontWeight="SemiBold"
                                    Text="Details:" />
                                <ListBox Height="165" />
                            </StackPanel>

                            <!--  Right Side: Chart  -->
                            <Border
                                Grid.Column="1"
                                Padding="15"
                                BorderBrush="#BDC3C7"
                                BorderThickness="1"
                                CornerRadius="5">
                                <StackPanel>
                                    <TextBlock
                                        Margin="0,0,0,10"
                                        FontSize="18"
                                        FontWeight="SemiBold"
                                        Text="New Members Over Time" />
                                    <lvc:CartesianChart Height="180" Series="{Binding NewMembersSeries}">
                                        <lvc:CartesianChart.AxisX>
                                            <lvc:Axis Title="Time" Labels="{Binding Labels}" />
                                        </lvc:CartesianChart.AxisX>
                                        <lvc:CartesianChart.AxisY>
                                            <lvc:Axis Title="Count" LabelFormatter="{Binding Formatter}" />
                                        </lvc:CartesianChart.AxisY>
                                    </lvc:CartesianChart>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <!--  Repeat same structure for New Books  -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="290" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                <TextBlock
                                    Margin="0,0,0,10"
                                    FontWeight="SemiBold"
                                    Text="Filter New Books" />

                                <StackPanel Margin="0,0,0,5" Orientation="Horizontal">
                                    <ComboBox
                                        Width="80"
                                        ItemsSource="{Binding Days}"
                                        SelectedItem="{Binding SelectedBookDay}"
                                        Style="{StaticResource CustomComboBoxStyle}" />
                                    <ComboBox
                                        Width="80"
                                        Margin="15,0"
                                        ItemsSource="{Binding Months}"
                                        SelectedItem="{Binding SelectedBookMonth}"
                                        Style="{StaticResource CustomComboBoxStyle}" />
                                    <ComboBox
                                        Width="80"
                                        ItemsSource="{Binding Years}"
                                        SelectedItem="{Binding SelectedBookYear}"
                                        Style="{StaticResource CustomComboBoxStyle}" />
                                </StackPanel>

                                <TextBlock
                                    Margin="0,10,0,5"
                                    FontWeight="SemiBold"
                                    Text="Details:" />
                                <TextBox
                                    Height="165"
                                    AcceptsReturn="True"
                                    Text="{Binding BookDetails}"
                                    TextWrapping="Wrap" />
                            </StackPanel>

                            <Border
                                Grid.Column="1"
                                Padding="15"
                                BorderBrush="#BDC3C7"
                                BorderThickness="1"
                                CornerRadius="5">
                                <StackPanel>
                                    <TextBlock
                                        Margin="0,0,0,10"
                                        FontSize="18"
                                        FontWeight="SemiBold"
                                        Text="New Books Added Over Time" />
                                    <lvc:CartesianChart Height="180" Series="{Binding NewBooksSeries}">
                                        <lvc:CartesianChart.AxisX>
                                            <lvc:Axis Title="Time" Labels="{Binding Labels}" />
                                        </lvc:CartesianChart.AxisX>
                                        <lvc:CartesianChart.AxisY>
                                            <lvc:Axis Title="Count" LabelFormatter="{Binding Formatter}" />
                                        </lvc:CartesianChart.AxisY>
                                    </lvc:CartesianChart>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <!--  Repeat same structure for Reservations  -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="290" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                <TextBlock
                                    Margin="0,0,0,10"
                                    FontWeight="SemiBold"
                                    Text="Filter Reservations" />

                                <StackPanel Margin="0,0,0,5" Orientation="Horizontal">
                                    <ComboBox
                                        Width="80"
                                        ItemsSource="{Binding Days}"
                                        SelectedItem="{Binding SelectedReservationDay}"
                                        Style="{StaticResource CustomComboBoxStyle}" />
                                    <ComboBox
                                        Width="80"
                                        Margin="15,0"
                                        ItemsSource="{Binding Months}"
                                        SelectedItem="{Binding SelectedReservationMonth}"
                                        Style="{StaticResource CustomComboBoxStyle}" />
                                    <ComboBox
                                        Width="80"
                                        ItemsSource="{Binding Years}"
                                        SelectedItem="{Binding SelectedReservationYear}"
                                        Style="{StaticResource CustomComboBoxStyle}" />
                                </StackPanel>

                                <TextBlock
                                    Margin="0,10,0,5"
                                    FontWeight="SemiBold"
                                    Text="Details:" />
                                <ListBox Height="165" />
                            </StackPanel>

                            <Border
                                Grid.Column="1"
                                Padding="15"
                                BorderBrush="#BDC3C7"
                                BorderThickness="1"
                                CornerRadius="5">
                                <StackPanel>
                                    <TextBlock
                                        Margin="0,0,0,10"
                                        FontSize="18"
                                        FontWeight="SemiBold"
                                        Text="Book Reservations Over Time" />
                                    <lvc:CartesianChart Height="180" Series="{Binding BookReservationsSeries}">
                                        <lvc:CartesianChart.AxisX>
                                            <lvc:Axis Title="Time" Labels="{Binding Labels}" />
                                        </lvc:CartesianChart.AxisX>
                                        <lvc:CartesianChart.AxisY>
                                            <lvc:Axis Title="Count" LabelFormatter="{Binding Formatter}" />
                                        </lvc:CartesianChart.AxisY>
                                    </lvc:CartesianChart>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>



                <!--  Placeholder for Members Management  -->
                <ScrollViewer
                    x:Name="MembersAdminPanel"
                    Margin="10,5,0,5"
                    Visibility="Collapsed">
                    <ScrollViewer.Resources>
                        <Style BasedOn="{StaticResource ModernScrollBarStyle}" TargetType="ScrollBar" />
                    </ScrollViewer.Resources>
                    <StackPanel>

                        <!--  Header  -->
                        <TextBlock Style="{StaticResource SectionHeader}" Text="Members Management" />
                        <TextBlock
                            Margin="0,0,0,20"
                            FontSize="16"
                            Foreground="#555"
                            Text="Manage library members, their accounts, and borrowing history." />

                        <!--  Search Bar  -->
                        <DockPanel Margin="0,0,0,15">
                            <TextBox
                                x:Name="MemberSearchBox"
                                Width="250"
                                Height="30"
                                VerticalAlignment="Center"
                                Style="{StaticResource ModernTextbox}"
                                Tag="Search by Username..." />
                            <Button
                                x:Name="MemberSearchButton"
                                Width="100"
                                Height="30"
                                Margin="0,0,340,0"
                                Background="#9370DB"
                                Click="SearchUser_Click"
                                Content="Search"
                                Foreground="White" />
                        </DockPanel>

                        <!--  Members Table  -->
                        <DataGrid
                            x:Name="MembersTable"
                            Height="460"
                            Margin="0,0,0,15"
                            AutoGenerateColumns="False"
                            BorderBrush="#ccc"
                            BorderThickness="1"
                            CanUserAddRows="False"
                            CanUserDeleteRows="False"
                            IsReadOnly="True">

                            <!--  Define columns explicitly  -->
                            <DataGrid.Columns>
                                <DataGridTextColumn
                                    Width="*"
                                    Binding="{Binding Username}"
                                    Header="Username" />
                                <DataGridTextColumn
                                    Width="2*"
                                    Binding="{Binding Email}"
                                    Header="Email" />
                                <DataGridTextColumn
                                    Width="*"
                                    Binding="{Binding Phone}"
                                    Header="Phone" />
                                <DataGridTextColumn
                                    Width="*"
                                    Binding="{Binding JoinDate}"
                                    Header="Join Date" />

                                <!--  Action Buttons  -->
                                <DataGridTemplateColumn Width="150" Header="Actions">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">

                                                <Button
                                                    Width="150"
                                                    Background="#9370DB"
                                                    Content="Insect"
                                                    Foreground="White"
                                                    Tag="{Binding}" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!--  Add New Member  -->
                        <GroupBox Margin="0,15,0,10" Header="Add New Member">
                            <StackPanel Margin="10">
                                <TextBox
                                    x:Name="NewMemberName"
                                    Margin="0,0,0,10"
                                    Style="{StaticResource ModernTextbox}"
                                    Tag="Username" />
                                <TextBox
                                    x:Name="NewMemberEmail"
                                    Margin="0,0,0,10"
                                    Style="{StaticResource ModernTextbox}"
                                    Tag="Email" />
                                <TextBox
                                    x:Name="NewMemberPhone"
                                    Margin="0,0,0,10"
                                    Style="{StaticResource ModernTextbox}"
                                    Tag="Phone Number" />
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock
                                        Margin="2,0,10,15"
                                        VerticalAlignment="Center"
                                        FontSize="12"
                                        Text="Password :" />
                                    <PasswordBox
                                        x:Name="NewMemberPassword"
                                        Width="590"
                                        Margin="0,0,0,10"
                                        Style="{StaticResource ModernPasswordbox}"
                                        Tag="Password" />
                                </StackPanel>

                                <Button
                                    x:Name="AddMemberButton"
                                    Width="120"
                                    Background="#9370DB"
                                    Click="AddMember_Click"
                                    Content="Add Member"
                                    Foreground="White" />
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>

                
                <!--  Placeholder for Events Management  -->
                <StackPanel
                    x:Name="EventsAdminPanel"
                    Margin="20"
                    Visibility="Collapsed">
                    <TextBlock Style="{StaticResource SectionHeader}" Text="Events Management" />
                    <TextBlock
                        FontSize="16"
                        Foreground="#555"
                        Text="Organize and manage library events and programs." />
                    <!--  Add event management UI elements here  -->
                </StackPanel>

            </Grid>
        </Border>
    </Grid>
</Page>