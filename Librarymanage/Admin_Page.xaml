<Page x:Class="Librarymanage.Admin_Page"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Librarymanage"
      mc:Ignorable="d" 
      d:DesignHeight="661" d:DesignWidth="984"
      Title="Admin_Page">

    <Grid Background="#F0F2F5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <Border Grid.Column="0" Background="#2C3E50" CornerRadius="0,10,10,0" Margin="0,10,5,10">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,30,0,0">
                <TextBlock Text="ADMIN PANEL" Foreground="#ECF0F1" FontSize="28" FontWeight="Bold" Margin="0,0,0,40" HorizontalAlignment="Center" />
                
                <Button Content="Books Management" Style="{StaticResource AdminNavButton}" Click="BooksButton_Click"/>
                <Button Content="Members Management" Style="{StaticResource AdminNavButton}" Click="MembersButton_Click"/>
                <Button Content="Ebook Management" Style="{StaticResource AdminNavButton}" Click="EbookButton_Click"/>
                <Button Content="User Management" Style="{StaticResource AdminNavButton}" Click="UserButton_Click"/>
                <Button Content="Events Management" Style="{StaticResource AdminNavButton}" Click="EventsButton_Click"/>
                <Button Content="Statistics" Style="{StaticResource AdminNavButton}" Click="StatsButton_Click"/>
                <Button x:Name="Go_Back" Content="Go Back" Style="{StaticResource AdminNavButton}" Margin="0,100,0,0" Click="BackButton_Click"/>
            </StackPanel>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Column="1" x:Name="ContentPanel" Background="#FFFFFF" Margin="5,10,10,10" CornerRadius="10">
            <Grid.Resources>
                <Style TargetType="Button" x:Key="AdminNavButton">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#ECF0F1"/>
                    <Setter Property="FontSize" Value="18"/>
                    <Setter Property="Height" Value="50"/>
                    <Setter Property="Margin" Value="0,5"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    <Setter Property="Padding" Value="20,0,0,0"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#34495E"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
                <Style TargetType="TextBlock" x:Key="SectionHeader">
                    <Setter Property="FontSize" Value="24"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Foreground" Value="#2C3E50"/>
                    <Setter Property="Margin" Value="0,0,0,20"/>
                </Style>
                <Style TargetType="TextBox">
                    <Setter Property="Height" Value="35"/>
                    <Setter Property="Padding" Value="5"/>
                    <Setter Property="BorderBrush" Value="#BDC3C7"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                </Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="#3498DB"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#2980B9"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Grid.Resources>

            <TextBlock Text="Welcome to the Admin Dashboard" Style="{StaticResource SectionHeader}" HorizontalAlignment="Center" VerticalAlignment="Center" />

            <!-- Books Management Panel -->
            <ScrollViewer x:Name="BookAdminPanel" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20">
                    <TextBlock Text="Add New Book" Style="{StaticResource SectionHeader}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,10">
                        <TextBox x:Name="AdminSearchBox" Width="300" Margin="0,0,10,0" PlaceholderText="Search by title or author"/>
                        <Button Content="Search Google Books" Click="AdminSearchButton_Click" Width="180"/>
                    </StackPanel>

                    <Border BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,20,0,0">
                        <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
                            <StackPanel x:Name="ApiBooksPanel" />
                        </ScrollViewer>
                    </Border>

                    <TextBlock Text="Books in Database" Style="{StaticResource SectionHeader}" Margin="0,40,0,0"/>
                    <Border BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Padding="10">
                        <ScrollViewer MaxHeight="400" VerticalScrollBarVisibility="Auto">
                            <StackPanel x:Name="DatabaseBooksPanel" />
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Statistics Panel -->
            <ScrollViewer x:Name="StatAdminPanel" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20">
                    <TextBlock Text="Library Statistics" Style="{StaticResource SectionHeader}"/>
                    <Grid Margin="0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- New Members -->
                        <Border Grid.Row="0" Grid.Column="0" BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,0,10,10">
                            <StackPanel>
                                <TextBlock Text="New Members Over Time" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                <lvc:CartesianChart Series="{Binding NewMembersSeries}" Height="180">
                                    <lvc:CartesianChart.AxisX>
                                        <lvc:Axis Title="Time" Labels="{Binding Labels}" />
                                    </lvc:CartesianChart.AxisX>
                                    <lvc:CartesianChart.AxisY>
                                        <lvc:Axis Title="Count" LabelFormatter="{Binding Formatter}" />
                                    </lvc:CartesianChart.AxisY>
                                </lvc:CartesianChart>
                            </StackPanel>
                        </Border>

                        <!-- New Books -->
                        <Border Grid.Row="0" Grid.Column="1" BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Padding="15" Margin="10,0,0,10">
                            <StackPanel>
                                <TextBlock Text="New Books Added Over Time" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                <lvc:CartesianChart Series="{Binding NewBooksSeries}" Height="180">
                                    <lvc:CartesianChart.AxisX>
                                        <lvc:Axis Title="Time" Labels="{Binding Labels}" />
                                    </lvc:CartesianChart.AxisX>
                                    <lvc:CartesianChart.AxisY>
                                        <lvc:Axis Title="Count" LabelFormatter="{Binding Formatter}" />
                                    </lvc:CartesianChart.AxisY>
                                </lvc:CartesianChart>
                            </StackPanel>
                        </Border>

                        <!-- Book Reservations -->
                        <Border Grid.Row="1" Grid.ColumnSpan="2" BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,10,0,10">
                            <StackPanel>
                                <TextBlock Text="Book Reservations Over Time" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                <lvc:CartesianChart Series="{Binding BookReservationsSeries}" Height="180">
                                    <lvc:CartesianChart.AxisX>
                                        <lvc:Axis Title="Time" Labels="{Binding Labels}" />
                                    </lvc:CartesianChart.AxisX>
                                    <lvc:CartesianChart.AxisY>
                                        <lvc:Axis Title="Count" LabelFormatter="{Binding Formatter}" />
                                    </lvc:CartesianChart.AxisY>
                                </lvc:CartesianChart>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <!-- Placeholder for Members Management -->
            <StackPanel x:Name="MembersAdminPanel" Visibility="Collapsed" Margin="20">
                <TextBlock Text="Members Management" Style="{StaticResource SectionHeader}"/>
                <TextBlock Text="Manage library members, their accounts, and borrowing history." FontSize="16" Foreground="#555"/>
                <!-- Add member management UI elements here -->
            </StackPanel>

            <!-- Placeholder for Ebook Management -->
            <StackPanel x:Name="EbookAdminPanel" Visibility="Collapsed" Margin="20">
                <TextBlock Text="Ebook Management" Style="{StaticResource SectionHeader}"/>
                <TextBlock Text="Manage free ebooks and their availability." FontSize="16" Foreground="#555"/>
                <!-- Add ebook management UI elements here -->
            </StackPanel>

            <!-- Placeholder for User Management -->
            <StackPanel x:Name="UserAdminPanel" Visibility="Collapsed" Margin="20">
                <TextBlock Text="User Management" Style="{StaticResource SectionHeader}"/>
                <TextBlock Text="Manage user accounts and permissions." FontSize="16" Foreground="#555"/>
                <!-- Add user management UI elements here -->
            </StackPanel>

            <!-- Placeholder for Events Management -->
            <StackPanel x:Name="EventsAdminPanel" Visibility="Collapsed" Margin="20">
                <TextBlock Text="Events Management" Style="{StaticResource SectionHeader}"/>
                <TextBlock Text="Organize and manage library events and programs." FontSize="16" Foreground="#555"/>
                <!-- Add event management UI elements here -->
            </StackPanel>

        </Grid>
    </Grid>
</Page>